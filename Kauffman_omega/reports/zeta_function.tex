\documentclass{jsarticle}
\usepackage{amsmath}
\usepackage{amssymb}
\begin{document}
\title{}
\author{}
\date{}
\maketitle

  $$ Zeta(p,q) = {{P_2 P_4 P_6 \cdots} \over {P_1 P_3 P_5 \cdots}} $$
  $$ = {{{{}_n C_{2n + 2}} \over {{}_n C_{2n + 1}}}} $$
  $$ = 2 n^2 - px + q $$
  $$ = (x - p)(x - q) + r $$
  $$ \chi(M) = \int_M 2|\sin 2x|^2dx \to (-1)^n \int \int \int_M
     f(x,y,z)dxdydz $$
  $$ \nabla (\square \phi)^{-K} \to \square(\nabla f) $$
  $$ \int \int {\partial \over \partial f} \left( \sigma \nabla(\square \phi)
    \otimes \right) F^{\oplus M}dx_m $$
  $$ \sum \to \bigoplus_{x \to 1}^{n} $$
  $$ \bigotimes \to \vee \chi (x) $$
  $$ \sum \left( [\oplus \phi 
	  [{\partial \over \partial f} \left(
     \sum (\square (\nabla \phi)) \right)]] \right)dvol $$
  $$ dvol \to dm_x, dx_m \to {d \over df}F $$
  $$ {d \over df} F = m(x) $$
  



\end{document}
