// Root build script. The Android Gradle Plugin version is pinned for
// reproducible CI builds (compatible with Gradle 8.7 in the wrapper).
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
