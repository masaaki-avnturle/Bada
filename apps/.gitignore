# Build outputs — the downloadable binaries are produced by CI, not committed.
build/
dist/
*.class
*.jar
# ...but the Gradle wrapper jar MUST be committed so CI's ./gradlew works.
!android/gradle/wrapper/gradle-wrapper.jar

# Gradle / Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/captures/
.gradle/
