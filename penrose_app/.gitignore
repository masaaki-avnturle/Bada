# dependencies / build outputs (regenerated by CI)
node_modules/
dist/
package-lock.json

# Capacitor generates the native Android project fresh in CI (npx cap add android)
android/
.gradle/
*.keystore
