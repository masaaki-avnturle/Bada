void address_set
{
    FILE *fp;
    address a[];
    

   _struct _{
   connecter *connect;
   connect.address_first_fp <- connect;
   operator_type.fp -> a[];
  } connecter_element
};

_struct _{
 Hash.defined <- connector_element.operator_type.scan(/^[{*."=>"*.}/).each|line|
 Array.defined <- connector_element.operator_type.scan(/^{[]}/).each|line|
 integer.defined <- connector_element.operator_type.scan(/d*./).each|line|
 String.defined <- connector_element.operator_type.scan(/w*./).each|line|
 Pointer.defined <- connector_element.operator_type.scan(/${fp})/.each|line|
 File.defined <- connector_element.operator_type.scan(/${File})/.each|line|
 Dir.defined <- connector_element.operator_type.scan(/${Dir})/.each|line|
} Regexpt_element

Scan(Regexpt_element).iterator <- DataBase[tuplespace]
Regexpt_element.developed :=> text.create.form
text.develop -> legal?(text.own) :=> text.form

detached.text <-> included.text
:=> {
      differentate.text :=> case :0
      then success exit -> all_detached
}


case fp.each_address_number
  /Array.address_number -> same_object_address/
  then x <- this.Array.insect
  /Integer.address_number -> same_object_address/
  then y <- this.Integer.insect
  x.metamorphose -> y
  x.address_set <- y
  puts(x) # [x y] represent this form.

x := y # x of address metamorphose to y and summatate this object array
       # integer hash file dir.
x <- y # y of address same object union x.
x <-> y # x and y of each object data is relativate with included object
        # data what belong.
[>> x , y] # inner of array object address 
           # from out of data to this array of x and y.
x -> y # x of address pointed what address belong of y.
x -< y # x of object data deroute for y of address develop.

x >- y # each of data like x routed to y of address represent
       # with belogn of object.
# these object address are exist which object of address is determinate
# with add or reject, even, derout, rout, catastrophe of each object address
# exists themselves.

