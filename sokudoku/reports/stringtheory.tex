\documentclass{jsarticle}
\usepackage[dvipdfmx,final]{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\begin{document}
\title{Secure product and quantum level equation}
\author{Masaaki Yamaguchi}
\date{}
\maketitle
   \newcommand{\variint}{%
	\begin{picture}(15,30)
		\put(0,0){
			$ \iint $}
		\put(0,5){\line(15,0){15}}
	\end{picture} %
}

\newcommand{\variiint}{%
	\begin{picture}(15,15)
		\put(0,0){
			$ \iiint $}
		\put(0,5){\line(15,0){20}}
	\end{picture} %
}



 \newcommand{\alearletter}{%
	 \begin{picture}(10,20)
		 \put(0,0){
			 $ \square $}
		 \put(0,0){\line(1,1){10}}
	 \end{picture} %
	 }

 \newcommand{\secproduct}{%
	 \begin{picture}(10,20)
		 \put(0,0){
			 $ \nabla $}
		 \put(5,0){+}
	 \end{picture} %
	 }

Quantum level equation equal with secure product,
and this product extend with gravity and newton equation
from scale expand of add formula.
	 $$ \secproduct = \bigoplus{(x^{\nabla})}^{\oplus}$$
      

$$  \scalebox{2}[2]{\secproduct}[\square]^{\nabla \psi} = $$
$$   \secproduct \square \cdot  \secproduct \nabla \le 
   \secproduct \square + \secproduct \nabla $$
$$ \scalebox{2}[2]{\secproduct}[\square]^{\nabla \psi}  $$
$$ = \scalebox{2}[2]{\secproduct}(\int C dx_m)^{i}d \tau $$
And, secure product of assemble partial integral equation.
$$ 2\int ||[\scalebox{2}[2]{\secproduct} \square(\nabla \psi)^{i}]^{
	\oplus \tau}||^{2}d \tau $$
$$ = \beta(p,q) $$
This formula is beta function constructed with circument formula.
$$ (\bigoplus {(i \hbar^{\nabla})}^{\nabla L} + m)
(\bigoplus {(i \hbar^{\nabla})}^{\nabla L} + n) $$
$$ = {L^{n+1} \over {n+1}} = \int (x - 1)^{1 - t}t^{x - 1}dx $$
$$ = \beta(p,q) $$
$$ {d \over d\gamma}\int \Gamma(\gamma)^{'}dx_m = \int \square \mathrm{dvol} $$

This equation is quantum level of deprivation being constructed with beta
function.
$$ \square \times \alearletter, E(k) = H(k) \times K(k) 
 \Psi \times H, \int {{k + 1} \over {m^{k+1}}}dm $$
Gravity and antigravity times equation are Thurston Perelman manifold,
and This equation is also beta function.
$$ \times(\chi(h))= v\pi(\chi,x), \sigma(x) + \sigma(y) = -{1 \over 2}
\int km^2 dm $$
And, this formula equal with D-brane, more also this equation Fuck equation.
$$ \sigma(\square + \alearletter) = E = H^{-1}(x)xH(x), 
{m}\times D^{n} = (i\hbar \psi)^{\ll D} $$
$$ S^{\ll p} \int \otimes h \nu = \int [D \psi \otimes h \nu]dm, 
 S^{m}:| x \to \Psi, \Psi(x)^{\ll p} $$ 
 These equations are D-brane of assemble with Sheap function.
$$= {}^{t}\scalebox{1.0}[2]{\text{$\variiint^{(p)}$}}\Psi (x) d \psi_m $$
$$ \int[Y]dm = (\bigotimes Y) \oplus (\bigotimes X) 
= - (X \cap Y) + (X \cup Y) $$
This material equation is Gauss sigunature of zeta function,
and this function construct with Lang-chain style formula.
$$ (D^{m} \otimes \nu m), \int \square^{\nabla}d \nu   
 = (\square^{\nabla})^{\oplus \nu} $$
$$ [{\secproduct} / {\square}], 
 [{\square} / {\alearletter}] $$
$$= {1 \over 2}(x_m + y_m) \ge \sqrt{x_m y_m} $$
Therefore, these equation are system with average of add and sqrt formula.

\end{document}
