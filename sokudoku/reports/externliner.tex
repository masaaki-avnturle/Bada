\documentclass{jsarticle}
\usepackage{amsmath}
\usepackage{amssymb}
\begin{document}
\title{}
\author{}
\date{}
\maketitle

  
  $$ \emptyset = \pi [{x \over y}]^{\mu\nu}  $$
  $$ \begin{bmatrix} \cos \theta && - \sin \theta \\
  \sin \theta && \cos \theta \end{bmatrix} \begin{bmatrix} \cos \theta && 
	  \sin \theta \\
  - \sin \theta && \cos \theta \end{bmatrix}  \begin{pmatrix} x \\
  y \end{pmatrix} = \begin{pmatrix} 1 && 0 \\
  0 && - i \end{pmatrix} $$
  $$ \pi [k_m \times e_m]^{\mu\nu} $$
  $$ = {d \over dk}{d \over de}(\pi {({x \over y})}) $$
  $$ z = \log {x \over y} = \log x - \log y $$
  $$ \mathrm{\Phi}k = \partial z = ({1 \over x} - {1 \over y})(x - y) $$
  $$ ||\Delta |x - y|||^{x} = \begin{pmatrix} 1 && 0 \\
  0 && -1 \end{pmatrix} $$
  $$ {d \over df}F = e^{x \log x} $$
  $$ = 0^{0^{'}} $$
  $$ = m(x) $$
  $$ = {d \over df}\int C dx_m $$





\end{document}
