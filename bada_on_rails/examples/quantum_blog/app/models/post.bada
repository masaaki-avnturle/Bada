# post モデル
# fields: は strong parameters (許可リスト) を兼ねる — ここに無い
# パラメータは db_create / db_update で黙って捨てられる。
# 宣言すると post_all / post_find / post_create / post_update /
# post_destroy / post_count のヘルパ作用素が自動定義される。

model :post, fields: ["title", "body", "status"]
