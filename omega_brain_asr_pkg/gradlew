#!/bin/sh
# Convenience wrapper for this app — forwards to the repository-root Gradle
# wrapper, scoped to this folder. Lets you run, e.g.:
#     ./gradlew assembleDebug
# from inside this app directory. The real wrapper lives at the repo root.
exec "$(dirname "$0")/../gradlew" "$@"
