# tuplespace.omega
module TupleSpace
  def init
    @store = []
  end
  def add(item)
    @store << item
  end
  def find(&blk)
    @store.select(&blk)
  end
  def parse(text)
    text.downcase
  end
end
