# Bada VM Pro OS — あなたのリポジトリの apt リポジトリ (flat 形式)
# 署名は付けず [trusted=yes] で許可 (GitHub Releases 上の Packages/Release を参照)。
# @APT_BASE_URL@ は build-iso.sh が実 URL に置換します。
deb [trusted=yes] @APT_BASE_URL@ ./
