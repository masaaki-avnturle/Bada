# default.mayu — 最小の既定設定
# 窓使いの憂鬱 の設定は keymap Global から始まる。
# この既定では CapsLock を Control として使えるようにするだけ。

# CapsLock を追加の Control 修飾キーにする
mod control += CapsLock

keymap Global
  # CapsLock 単独打鍵の素通しは抑止 (修飾子としてのみ働く)
  key CapsLock = &Ignore
