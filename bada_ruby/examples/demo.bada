# demo.bada — a Bada language script (run with: bin/bada run examples/demo.bada)
#
# Operators:
#   <-   left non-commutative act   π(χ,x) = [iπ, f(x)]
#   -<   manifold integral          ∬ 1/(x log x)^2 dx_m
#   >-   quantum right act          e^{-x log x}
#   Omega::push  /  Ω::push         write to the Akashic TupleSpace

set g = 2.5
g <- "global differential manifold entropy"
print g

g -< 3.0
print g

g >- g
print g

Omega::push g as manifold_node
print g
