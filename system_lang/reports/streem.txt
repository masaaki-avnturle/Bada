file << gets
file = file.to_a
file.each{|line|
  Thread.start do
    system(line)
  end
}

Dir.open(directory_name,"a+"){|dir|

  Thread.start do
    while dir != nil
     File.create(target){|target|
      target << system(ps auxf)
      target.each do |line|
       system(kill line)
        if line ~= nil
          then
          break
        end
      end
     } 
    end
  end
}


