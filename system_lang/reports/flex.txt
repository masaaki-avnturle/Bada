i << file[count]
low_algorism = primary[i],cond[i]
loop {
  case low_algorism.each do |line|
  when line ~= / /
    exec pattern.scan(/ /) do |spec|
      if spec ~= / /
        spec.category.match
        then 
        if category.match == array
          print array.element
        else if
          category.match == hash
          print hash.element
        else if
          category.match == string
          print string.element
        else if
          category.match == integer
          print integer.element
        else if
          category.match == charastic
          print char.element
        else if
          category.match == block
          print block.element
        else if
          category.match == 'class'
          print 'class'.element
        else if
          category.match == 'def'
          print 'def'.element
        else if
      end
    end
  end
  end
}

primary: ADD add <- +
    |: DEL del <- -
    |: Qual qual <- %
    |: Times times <- *
    |: ARRAY array <- []
    |: HASH hash <- {} 
    |: STRING string <- " "
    |: INTEGER integer <- number
    |: FLOAT floart <- point
    |: DOUBLE double <- point
    |: ATTRI  attri <- propity
    |: CLASS  class <- one
    |: DEF    def <- establish
}

 condition |: class -> def -> ENDIRE[]
           |: <- end
 ENDIRE = {+,-,*,%,class,def,| |,||,|,&,{},do,while,
     if,when,scan,match,int,char,[ ]," ",' ',iterator,
     loop,while,regexpt,
 }

   primary[i].each do |i|
     i ~= element -> ENDIRE[x].mixed => condition[y]
   end

     //condition[y] is entirely lang construct with
     //all of style of klass
     //therefore, this klass of structure invloved with
    //ENDIRE element in this programing language.

def < loop{value[i]}
  class < def
    value[i].each do |line|
      exec line <-> SUM(line).chain -> print SUM(line)
    end
  end
}

def.each do |method|
  exec system.method 
end
loop{
 def.each do |method|
  def.first.method end : def.second.method end : 
  def.third.method end : def.fourth.method end
    def.these -> exec united_method :+ 
      print these.method
    end
 end
}



class < def.included_xp
  class.each do |klass|
    category.klass <-> class.property => array.hierarchy
    this.array <-~ categroy.klass
    exec klass[array.hierarchy]
  end
}
