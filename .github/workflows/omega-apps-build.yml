name: Ω apps build (APK + Windows EXE)

# bio_medicine 配下の各アプリ(www + electron + cordova)から Android APK と Windows EXE をビルドし、
# Actions アーティファクト、および (apps-v* タグ時) GitHub Release に添付します。
#
# ⚠ いずれのアプリも 概念シミュレーション/アート・非医療 です。

on:
  workflow_dispatch:
  push:
    tags:
      - 'apps-v*'

permissions:
  contents: write

jobs:
  windows-exe:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        app: [omega_biofeedback, omega_thermal_trace, omega_telepathy, omega_badagpt]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Build Windows EXE (Electron)
        working-directory: bio_medicine/${{ matrix.app }}/electron
        run: |
          npm install
          npm run dist
      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.app }}-windows
          path: bio_medicine/${{ matrix.app }}/electron/dist/*.exe
      - name: Attach EXE to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: bio_medicine/${{ matrix.app }}/electron/dist/*.exe

  android-apk:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app: [omega_biofeedback, omega_thermal_trace, omega_telepathy, omega_badagpt]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
      - name: Install Android SDK packages (+ accept licenses)
        run: |
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager "platform-tools" "platforms;android-33" "build-tools;33.0.2"
      - name: Pin Gradle 8.7 (cordova-android 12 needs Groovy 3; runner ships Gradle 9/Groovy 4)
        run: |
          set -e
          # The hosted runner ships Gradle 9.x (Groovy 4), where cordova-android 12's
          # cordova.gradle fails: "unable to resolve class XmlParser" (no longer auto-imported).
          # Pin Gradle 8.7 (Groovy 3) and put it first on PATH so Cordova uses it.
          curl -sSL https://services.gradle.org/distributions/gradle-8.7-bin.zip -o /tmp/gradle.zip
          sudo unzip -q -o /tmp/gradle.zip -d /opt
          echo "GRADLE_HOME=/opt/gradle-8.7" >> "$GITHUB_ENV"
          echo "/opt/gradle-8.7/bin" >> "$GITHUB_PATH"
      - name: Build debug APK (Cordova, pinned versions)
        run: |
          set -e
          echo "Using gradle: $(which gradle)"; gradle --version | head -5
          npm install -g cordova@12.0.0
          APP="bio_medicine/${{ matrix.app }}"
          cordova create cordova-build com.bada.omega TmpApp
          rm -rf cordova-build/www
          cp -r "$APP/www" cordova-build/www
          cp "$APP/cordova/config.xml" cordova-build/config.xml
          cd cordova-build
          cordova platform add android@12.0.1
          cordova build android --verbose
      - name: Collect APK
        run: |
          mkdir -p out
          cp cordova-build/platforms/android/app/build/outputs/apk/debug/*.apk "out/${{ matrix.app }}-debug.apk"
          ls -la out
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.app }}-android
          path: out/*.apk
      - name: Attach APK to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: out/*.apk
