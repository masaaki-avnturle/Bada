name: Build BadaSokudoku APK

# Builds the BadaSokudoku speed-reading app and publishes the APK so it can be
# downloaded from this repository:
#   - every build uploads the APK as a workflow "Artifact"
#     (Actions run page -> Artifacts -> BadaSokudoku-apk)
#   - pushing a tag like v1.0 also attaches the APK to a GitHub Release
#     (Releases page -> download BadaSokudoku-debug.apk)

on:
  push:
    branches: [ "main", "claude/biomedical-monitoring-app-SNqVR" ]
    paths:
      - "bada_sokudoku/**"
      - ".github/workflows/android-apk.yml"
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bada_sokudoku
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK packages
        run: sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Stage APK
        run: cp app/build/outputs/apk/debug/app-debug.apk BadaSokudoku-debug.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: BadaSokudoku-apk
          path: bada_sokudoku/BadaSokudoku-debug.apk
          if-no-files-found: error

      - name: Attach APK to Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: bada_sokudoku/BadaSokudoku-debug.apk
