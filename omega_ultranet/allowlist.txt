# Optional IP allowlist for `omeganet.py host --allow allowlist.txt`.
# One IP per line. Connections from these IPs are auto-approved (still logged).
# Anyone NOT listed must be approved manually at the host console.
# Lines starting with '#' are ignored.
#
# Example:
# 192.168.1.42
# 203.0.113.7
