// ルートビルドスクリプト — Android Gradle Plugin のみ。
// 依存ライブラリゼロ (AndroidX/appcompat 不使用) で APK を最小化し、
// ストレージ/メモリの少ないタブレットでも動くようにしています。
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
