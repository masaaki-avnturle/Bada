# MadoKey 常駐に必要な依存
pynput>=1.7            # グローバル キー待ち受け / キー送出 (全OS)

# Windows で Word/Excel の ExecuteMso (mso アクション) を使う場合:
pywin32>=305 ; platform_system == "Windows"
psutil>=5.9  ; platform_system == "Windows"

# LibreOffice の uno アクションは、OS 同梱の python-uno を使います
# (Ubuntu: `sudo apt install libreoffice-script-provider-python` / python3-uno)。
# soffice を次のように起動しておくと確実に接続できます:
#   soffice --calc --accept="socket,host=localhost,port=2002;urp;"
