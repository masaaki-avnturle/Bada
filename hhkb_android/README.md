# Bada HHKB Pro — フローティング Android キーボード

Happy Hacking Keyboard **Pro** 風のレイアウトを再現し、**画面上を自由にドラッグ
移動できる** Android 12（API 31）対応のソフトウェアキーボード（IME）です。

Esc / Tab / Control / Shift / Alt / Meta(◇) / Fn など特殊キーをすべて搭載し、
Fn レイヤーで矢印・ファンクションキー・ナビゲーションキーも入力できます。

---

## 特長

- **Android の「入力方法（IME）」として登録可能**
  設定 → システム → 言語と入力 → 画面キーボード から有効化できます。
- **HHKB Pro (US) 配列を忠実に再現**（60キー / 5段）。
- **特殊キー一式**：`Esc` `Tab` `Control` `Shift` `Alt` `◇(Meta/Super)` `Fn`
  `Return` `Delete` `Space`。
- **US / 日本語(JIS) レイアウト切替**：上部バーの `US`/`日本語` ボタンで、
  **HHKB Pro (US/ANSI) 配列** と **HHKB Professional JP (JIS) 配列** を物理配列ごと切替。
  JIS では `¥` `^` `@` `:` `\(ろ)` キーと JIS のシフト記号（`2→"`,`6→&`,`7→'` …）になり、
  無変換/変換/あ-ア/全半キー＋かな漢字変換が有効。
  **日本語に切り替えると即ひらがな入力**になり、ローマ字がそのまま かな に変換されます
  （`A` を押せば日本語配列のまま英数入力も可能）。起動時の既定もひらがなです。
- **日本語入力キー**：`無変換` `変換`、`あ/A/ア`（英数⇔ひらがな⇔カタカナ）、`全/半`。
  辞書不要のローマ字かな入力＋内蔵辞書の漢字変換（詳細は下記）。
- **専用矢印キー**：最下段に `← ↑ ↓ →`（Fnなしでカーソル移動・変換候補移動）。
- **Jones多様体セキュリティ `🔐`**：選択（またはカーソル前）のテキストを、結び目理論
  （Kauffman括弧／Jones多様体）由来の鍵で暗号化／復号。`omega_jones_crypto` と同系の
  encrypt-then-MAC（HMAC-SHA256）でオンデバイス処理（下記）。
- **スティッキー修飾キー**：
  - 1回タップ … 次の1キーだけ有効（ワンショット、薄い金色）
  - 2回タップ … ロック（固定、金色反転）
  - 3回タップ … 解除
  これにより `Ctrl+C` などの同時押しを片手・順次タップで実現します。
- **フローティング＆ドラッグ移動**：上部の `⠿ ハンドル` バーをドラッグすると
  キーボードごと画面の好きな位置へ移動。`Dock` ボタンで画面下中央に戻ります。

---

## APK の入手（ビルド済みパッケージ）

このリポジトリには **GitHub Actions による自動APKビルド**（`.github/workflows/android.yml`）が
含まれており、`hhkb_android/` への push ごとに APK が生成されます。

### ① Actions の成果物からダウンロード（最も簡単）

1. GitHub の **Actions** タブ →「Build HHKB APK」の最新の成功した実行を開く。
2. 画面下部 **Artifacts** の `BadaHHKB-debug-apk` をダウンロード（zip）。
3. zip を展開すると `app-debug.apk` が得られます。

### ② Release から直接ダウンロード

GitHub で **Release を作成**すると、同じワークフローが APK を Release に添付します
（単一ファイルとして直接ダウンロード可能）。

### APK のインストール

1. `app-debug.apk` を Android 12 端末へ転送。
2. 「提供元不明のアプリ／このソースを許可」を有効にしてインストール。
   （デバッグ署名済みなので、そのままインストールできます。）

## 自分でビルドする場合

> JDK 17 と Android SDK（platform 34 / build-tools 34）が必要です。
> Gradle Wrapper を同梱しているので Gradle の事前インストールは不要です。

- **Android Studio**：`hhkb_android/` を開く → Gradle 同期 → Run。
- **コマンドライン**：

```bash
cd hhkb_android
./gradlew assembleDebug   # app/build/outputs/apk/debug/app-debug.apk
```

---

## 使い方

1. アプリ「**Bada HHKB**」を起動。
2. **① 入力方法の設定を開く** → 『Bada HHKB Pro Keyboard』を ON。
3. **② キーボードを切り替える** → 一覧から Bada HHKB を選択。
4. 任意のアプリの入力欄で HHKB キーボードが表示されます。
   - ハンドルバーをドラッグして移動。
   - `Dock` で下中央へ復帰。

---

## キー配列

### US（ANSI）
```
 Esc  1! 2@ 3# 4$ 5% 6^ 7& 8* 9( 0) -_ =+ \| `~
 Tab    Q  W  E  R  T  Y  U  I  O  P  [{ ]}   Delete
 Ctrl     A  S  D  F  G  H  J  K  L  ;: '"      Return
 Shift      Z  X  C  V  B  N  M  ,< .> /?    Shift  Fn
 ◇  Alt        [        Space        ]   Alt   ◇
```

### 日本語（JIS / HHKB Professional JP）
```
 Esc  1! 2" 3# 4$ 5% 6& 7' 8( 9) 0  -= ^~ ¥|  Delete
 Tab    Q  W  E  R  T  Y  U  I  O  P  @` [{
 Ctrl     A  S  D  F  G  H  J  K  L  ;+ :* ]}   Return
 Shift      Z  X  C  V  B  N  M  ,< .> /? \_  Shift Fn
 ◇  Alt  無変換   [   Space   ]   変換   あ/A   全/半   ← ↑ ↓ →
```

## Jones多様体セキュリティ（🔐）

上部バーの `🔐` で、**選択テキスト**（無ければカーソル前のテキスト）を暗号化／復号します。

- 鍵は**結び目図（トレフォイル）の Kauffman 括弧**を複数の A 値で評価し SHA-256 で
  32バイト化（Jones多項式／Jones多様体に基づく導出。`omega_jones_crypto` と同方式）。
- 暗号は **encrypt-then-MAC**（HMAC-SHA256 鍵ストリーム＋HMAC-SHA256 タグ、改ざん検知付き）。
- 暗号文は `🔐` で始まる Base64。同じ `🔐` を押すと復号されます。すべて端末内処理。
- 添付理論（global partial integral manifold / 非可換Lie・RSA秘密データ）に対応した
  「Happy Hacking Keyboard セキュリティ」機能です。

### 日本語入力（変換 / 無変換 / 日英切り替え）

| キー | 動作 |
|:--|:--|
| `あ/A`（モード） | 押すたびに **A(英数) → あ(ひらがな) → ア(カタカナ)** を循環。カタカナモードでは入力中から直接カタカナ表示 |
| （日本語モードで英字） | ローマ字 → ひらがなにリアルタイム変換（未確定文字列として表示） |
| `全/半` | **全角 / 半角**を切り替え（押すとラベルが 全⇄半、全角時は金色）。変換候補の幅優先と、英数・記号の直接入力幅に反映 |
| `変換` / `Space` | 未確定文字列を変換し、**漢字 → ひらがな → カタカナ(全/半) → 英字(全/半)** を循環。上部の候補バーに一覧表示 |
| `Return` | 選択中の候補で確定 |
| 候補バーをタップ | その候補で確定 |
| `無変換` | 変換せず **ひらがなで確定** |
| `Delete` | 変換中なら変換を取消（読みに戻る）／未確定なら1文字削除 |

#### 漢字変換について（無料の大規模辞書）

- 起動時に小さな内蔵辞書 `assets/jadict.tsv` で即変換でき、**初回にバックグラウンドで
  無料の SKK 辞書（`SKK-JISYO.ML`, GPL / 約4万語）をダウンロード**して、以降は
  はるかに豊富な漢字変換になります（`にほん→日本/二本`、`かんじ→漢字/幹事/感じ/完治…`、
  `あい→愛/哀/相/藍…`）。
- 出典：[skk-dev/dict](https://github.com/skk-dev/dict)（SKK辞書, EUC-JP, GPL）。
  読み（ひらがな）→ 候補の対応表をそのまま利用。完全オフラインで動作（DL後）。
- さらに語彙を増やしたい場合は `SKK-JISYO.L`（約17万語）に差し替え可能（`DictionaryRepository.SKK_URL`）。
- 旧来の `assets/jadict.tsv`（`読み<TAB>漢字1,漢字2,…`）も併用され、独自語の追記に使えます。
- 変換例：`にほん`→日本、`かんじ`→漢字／感じ／幹事、`へんかん`→変換／返還、
  `たべる`→食べる、`がっこう`→学校。
- 文節分割は行わず、入力中の読み全体を一括変換します（軽量・オフライン動作）。
  辞書に無い語はひらがな／カタカナ／英字（全角・半角）の候補のみ表示されます。

#### 全角 / 半角

- `全/半` キーで全角・半角を切替。変換候補に **全角カタカナ（カタカナ）/ 半角カタカナ（ｶﾀｶﾅ）/
  全角英字（ａｂｃ）/ 半角英字（abc）** がすべて含まれ、`全/半` の状態で優先順が変わります。
- 英語モードや、日本語モードでの数字・記号の直接入力にも全角／半角が反映されます
  （例：全角時 `1` → `１`、`A` → `Ａ`）。
- **カタカナモード（ア）では `全/半` で直接入力が「カタカナ」⇄「ｶﾀｶﾅ」に切り替わります**
  （変換中に `全/半` を押すと表示も即時に切替）。
- **全角モードのスペースは全角スペース（　）** になります。
- 変換例：`kanji`→ 漢字 / かんじ / ｶﾝｼﾞ / カンジ / ｋａｎｊｉ / kanji。
  カタカナモードで `katakana`→ カタカナ（全角）/ ｶﾀｶﾅ（半角）。

### ホログラム表示（Holo）

- 上部バーの **`Holo`** ボタンで、キーボードをホログラム風(半透明＋ネオン発光)の
  スキンに切り替えます。暗所で透明アクリル板／ピラミッドに映すと浮遊表示にも使えます。
- ※ スマートフォン単体で本物のレーザー投影ホログラムは出せないため、これは
  「ホログラム風の視覚表現」です。

### 辞書（漢字 / 英英）— Webから無料ダウンロード

上部バーの **`辞`** ボタンで、カーソル直前の語を辞書検索してパネル表示します。

| 種別 | 辞書 | ライセンス | 取得元 |
|:--|:--|:--|:--|
| 漢字 | **KANJIDIC2**（音訓・意味・画数） | CC BY-SA（© EDRDG） | raw.githubusercontent.com |
| 英英 | **Webster's 1913**（英→英の語義） | パブリックドメイン | raw.githubusercontent.com |

- 直前が漢字なら各字の読み・意味・画数を、英単語なら英英の語義を表示します。
- 辞書データは**初回に自動ダウンロード**して端末内に保存（オフラインで再利用）。
  そのため `INTERNET` 権限を使用します。すべてオンデバイス参照です。

> 注：ご要望の **漢語林**（大修館書店）は商用辞書で無料配布されていないため、
> 同等の**無料・オープン**な漢字辞典 **KANJIDIC2** に置き換えています。

- ローマ字例：`nihongo`→にほんご、`konnichiha`→こんにちは、`kya`→きゃ、
  `tte`→って、`gakkou`→がっこう、`pen`→ぺん、`annai`→あんない。
- `ん` は単独 `n`（語末）／`n`＋子音／`nn` で入力できます（例 `shinbun`→しんぶん）。
- `変換`/`無変換` は **未確定文字列が無いとき** は本物の HENKAN / MUHENKAN
  キーイベントを送出するので、他社IMEと併用する物理JISキーボード的な使い方も可能です。
- 辞書を内蔵しないため**漢字変換は行いません**（ひらがな/カタカナ/全角英字の確定変換）。
  漢字が必要な場合は本キーボードで読みを入力し、別途漢字変換IMEと併用してください。

### Fn レイヤー早見表

| Fn + キー | 機能 |
|:--|:--|
| `1`〜`9` `0` | F1〜F9, F10 |
| `-` `=` | F11, F12 |
| `[` | ↑ (Up) |
| `/` | ↓ (Down) |
| `;` | ← (Left) |
| `'` | → (Right) |
| `,` | Home |
| `.` | End |
| `]` | Page Up |
| `Space` | Page Down |
| `\` | Insert |
| `Tab` | Caps Lock |
| `Delete` | 前方削除 (Forward Delete) |

> `Ctrl` / `Alt` / `◇(Meta)` を有効にした状態で文字キーを押すと、
> `Ctrl+C` / `Alt+Tab` / `Meta+L` などのキーイベントとしてアプリへ送信されます。

---

## 構成

| ファイル | 役割 |
|:--|:--|
| `HhkbImeService.java` | IME 本体。レイアウト生成・修飾キー処理・キー送信・ドラッグ移動。 |
| `SetupActivity.java` | 有効化案内とキーボード切替ボタンを持つ起動画面。 |
| `res/xml/method.xml` | IME ディスクリプタ（サブタイプ定義）。 |
| `AndroidManifest.xml` | IME サービスと起動 Activity の登録。 |

外部依存なし（Android フレームワークの `InputMethodService` のみ）。
